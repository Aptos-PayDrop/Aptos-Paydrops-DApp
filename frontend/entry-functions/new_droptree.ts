import { InputTransactionData } from "@aptos-labs/wallet-adapter-react";
import {
  APT_DECIMALS,
  convertAmountFromHumanReadableToOnChain,
  convertAmountFromOnChainToHumanReadable,
} from "@/utils/helpers";

export const newDroptree = (args: any): InputTransactionData =>{
    
}